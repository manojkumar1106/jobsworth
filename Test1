protected Map<String, Object> executeSingle(String procName, Map<String, Object> params) {
    List<Map<String, Object>> rows = executeList(procName, params);
    return rows.isEmpty() ? null : rows.get(0);
}
