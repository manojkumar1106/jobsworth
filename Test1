private static String generateIamAuthToken(
        String clusterName, String userId, Region region) throws Exception {

    final var requestUri = URI.create(
        String.format("http://%s/", clusterName)
    );

    SdkHttpFullRequest request = SdkHttpFullRequest.builder()
        .method(SdkHttpMethod.GET)
        .uri(requestUri)
        .appendRawQueryParameter("Action", "connect")
        .appendRawQueryParameter("User", userId)
        .build();

    // Sign the request
