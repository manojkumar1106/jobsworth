drl.append("\n            )\n");   // <-- this closes Cart(...)

// ADD THIS BLOCK HERE:
if (sd.getMinQuantity() != null && sd.getMinQuantity() > 0) {
    drl.append("        $totalQty : Number(intValue >= ")
       .append(sd.getMinQuantity())
       .append(") from accumulate(CartItem($qty : quantity) from $cart.items, sum($qty))\n");
}

drl.append("    then\n");   // <-- this was already there

